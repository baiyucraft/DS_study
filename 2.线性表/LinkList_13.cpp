#include <stdio.h>
#include <stdlib.h>
#include "LinkList.h"

// 合并两个递增有序的单链表L1、L2，合并成递减（采用头插法）
void MergeLinkList(LinkList& L1, LinkList& L2) {
	// 合并链表的结点
	LNode* r;
	// L1的
	LNode* p1 = L1->next;
}